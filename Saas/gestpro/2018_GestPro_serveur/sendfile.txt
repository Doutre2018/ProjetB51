daemon.register_function(self.sendFile)

def sendFile(self, cheminFichier):
        with open(cheminFichier, "rb") as fichier:
            return xmlrpc.client.Binary(fichier.read())


client side:
with open("fetched_file.jpg", "wb") as handle:
    handle.write(proxy.sendFile("batman.jpg").data)
